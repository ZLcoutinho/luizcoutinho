import firebase from 'firebase/app'
import 'firebase/firestore'

const  WriteToCloudFirestore = () => {

    const sendDate = () => {
        try {

        } catch (error) {
            console.log(error)
            alert(error)
        }
    }

    return (
        <div>

        </div>
    )
}